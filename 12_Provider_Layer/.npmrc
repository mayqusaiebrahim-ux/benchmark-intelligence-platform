# See 11_Benchmark_Engine/.npmrc and the project root's .npmrc for the full
# explanation. This package has its own separate playwright install
# (BrowserSessionManager.js's chromium.launch() resolves through this
# copy, not the Engine's) and its own `--prefix`-scoped npm config
# resolution, so it needs this file too.
playwright_browsers_path=0
